#!/bin/zsh

# Work profile credential template. Copy with:
# make PROFILE=work secrets-init
#
# Add only employer-approved credentials. Do not copy personal tokens into this
# file, even when a variable name is shared between profiles.

export OPENAI_API_KEY=""
export ANTHROPIC_API_KEY=""
export DEEPGRAM_KEY=""
