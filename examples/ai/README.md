# AI configuration examples

These files are generic examples, not live application state. Review them
before copying anything into Claude, Codex, or Gemini configuration directories.

They intentionally omit account authentication, project trust lists, absolute
paths, private permissions, plugin state, session data, and local histories.
